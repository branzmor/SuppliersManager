server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # SPA fallback so client-side routing (if any is added later) doesn't 404 on refresh.
        try_files $uri $uri/ /index.html;
    }
}

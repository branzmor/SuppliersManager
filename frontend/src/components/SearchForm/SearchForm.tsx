export interface SearchFormProps {
  onSearch: (rate: number) => void;
}

// TODO: implement — numeric "amount" input (README: minimum 250) + search button, with a
// validation message shown when the value is below 250 (do not call onSearch in that case).
export function SearchForm(_props: SearchFormProps) {
  return null;
}
